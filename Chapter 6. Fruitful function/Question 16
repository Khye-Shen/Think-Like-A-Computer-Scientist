#Write a function is_factor(f, n) that passes these tests:

#is_factor(3, 12)
#not is_factor(5, 12)
#is_factor(7, 14)
#not is_factor(7, 15)
#is_factor(1, 15)
#is_factor(15, 15)
#not is_factor(25, 15)

def is_factor(f, n):

    if n % f == 0:

        return "True"

    else:

        return "False"

factor = is_factor(3, 11)

print(factor)
